#ifndef CLONE_SEQUENCE_H
#define CLONE_SEQUENCE_H

/**
 * Writes the first `n` Fibonacci numbers into `out` (which must hold at
 * least `n` entries) and returns the number of values written.
 */
int clone_sequence(int n, int *out);

#endif
